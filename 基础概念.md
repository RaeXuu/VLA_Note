### **Parquet**（全称 Apache Parquet）

是一种开源的**列式存储（Columnar Storage）**文件格式，主要设计用于在大数据处理系统中提高数据查询效率和降低存储成本。

如果把传统的 CSV 或数据库表比作“按行堆叠”的砖块，Parquet 则是将“相同颜色的砖块”分开放置。

  

### **JSON**（JavaScript Object Notation）

是一种轻量级的**数据交换格式**。虽然它起源于 JavaScript，但目前已经成为一种完全独立于编程语言的标准格式，几乎所有现代编程语言（如 Python, C++, Java, Go 等）都支持它。简单来说，JSON 就是一种**用易于阅读的文本来存储和传输数据对象**的方式。

**核心特点**

1. 易于阅读和编写： 采用了类似于编程语言中“键值对”的结构，人类一眼就能看懂。
    
2. 轻量级： 相比于 XML，JSON 的冗余字符更少，传输效率更高。
    
3. 跨语言支持： 它是标准化的，不同平台、不同语言之间可以通过 JSON 轻松交换数据。
    

**基本语法规则**

JSON 的结构主要由两种形式组成：

1. **对象（Object）：** 用花括号 `{}` 包围，里面是 `key: value` 形式的键值对。
    
2. **数组（Array）：** 用方括号 `[]` 包围，里面是值的有序列表。
    

**支持的数据类型包括：**

- 字符串（需用双引号 `""`）
    
- 数字（整数或浮点数）
    
- 布尔值（`true` 或 `false`）
    
- 空值（`null`）
    
- 对象或数组（可以嵌套）
    

  

### **OOD**

Out of distruction

  

### **batch**

**定义：** 一次前向传播 + 反向传播所使用的一小批训练样本。

**核心作用：**

控制 显存占用

控制 梯度估计稳定性

  

### **epoch**

**定义：**

模型完整地看完 **整个训练数据集一次**

  

### **episode**

**定义：**

从环境 reset 开始，到任务结束的一整段交互轨迹

**结构**：

episode = { s0, a0, r0 s1, a1, r1 ... sT }

  

### **Chunk**

**定义**通常是：

把长序列数据切成小段