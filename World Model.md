# 背景

World Model 最早起源于 **基于模型的强化学习** (Model-based RL)，旨在通过内部世界建模来提升采样效率。代表性工作包括 **Dreamer** 系列（Dreamer, DreamerV2, DreamerV3），通过学习潜在动态模型，实现“在脑中想象未来”式的策略更新。

在具身智能的最新语境中，World Model 的概念被拓展为 **条件视频生成模型** (conditioned video generation model)，用于模拟未来观测、预测任务后果，并与规划模块或语言模型结合以实现长期推理。

# **四大路线**

1. ## 联合嵌入JEPA
    

2. ## 空间智能 Li Feifei
    

### **From Words to Worlds: Spatial Intelligence is AI’s Next Frontier**

对于空间智能，我通过**三种基本能力来定义世界模型：**

#### 1.**生成式：**世界模型可以生成在感知、几何和物理上都保持一致的世界。

能够解锁空间理解和推理的世界模型，也必须能够生成自身的模拟世界。它们必须能够生成无穷无尽、丰富多样的模拟世界，这些世界遵循语义或感知指令，_同时在_几何、物理和动态上保持一致——无论代表的是真实空间还是虚拟空间。研究界正在积极探索这些世界应该以隐式还是显式的方式，根据内在的几何结构来表示。此外，除了强大的潜在表征之外，我认为通用世界模型的输出还必须允许为许多不同的应用场景生成一个显式的、可观察的世界状态。尤其重要的是，它对当下的理解必须与过去紧密相连；与导致当前状态的先前世界状态紧密相连。

#### 2.**多模态：**世界模型从设计上就是多模态的。

就像动物和人类一样，世界模型应该能够处理各种形式的输入——在生成式人工智能领域被称为“提示”。给定部分信息——无论是图像、视频、深度图、文本指令、手势还是动作——世界模型都应该尽可能_完整地预测或生成世界状态_。这要求模型能够以媲美真实视觉的精度处理视觉输入，并同样轻松地解读语义指令。这使得智能体和人类都能通过各种输入与模型交流，并接收各种输出。

#### 3.**交互式：**世界模型可以根据输入动作输出下一个状态

最后，如果行动和/或目标作为世界模型的输入，其输出必须包含世界的_下一个_状态，无论该状态是以隐式还是显式的方式表示。当仅输入行动（无论是否包含目标状态）时，世界模型应生成与世界先前状态、预期目标状态（如有）及其语义含义、物理定律和动态行为相一致的输出。随着空间智能世界模型的推理和生成能力日益强大和稳健，可以设想，在给定目标的情况下，世界模型本身不仅能够预测世界的下一个状态，还能基于新状态预测下一步行动。

  

### **RTFM**

- **效率**：RTFM 仅使用单个 H100 GPU 即可以交互式帧速率运行推理。
    
- **可扩展性**：RTFM 的设计旨在随着数据和计算能力的增长而扩展。它无需依赖显式的 3D 表示即可对 3D 世界进行建模，并使用可从大规模视频数据中学习的通用端到端架构。
    

RTFM采用了一种不同的方法。它基于生成式视频建模的最新进展，训练一个神经网络，该网络输入一个或多个场景的二维图像，并从新的视角生成该场景的二维图像，而无需构建任何显式的三维世界表示。RTFM实现为一个自回归扩散变换器，该变换器处理帧序列，并在大规模视频数据上进行端到端训练，以根据前一帧预测下一帧。

RTFM 可以被视为一个学习型渲染器。它的输入帧被转换为神经网络激活值（KV 缓存），这些激活值隐式地表示了世界；在生成新帧时，网络会从这种表示中读取信息（通过注意力机制），从而创建与输入视图一致的新世界视图。将输入视图转换为世界表示，然后从这些表示中渲染新帧的机制，都是从数据中端到端学习得到的，而不是手动设计的。RTFM 通过在训练过程中观察，就能学习如何对反射和阴影等复杂效果进行建模。

RTFM模糊了**重建（在现有视图之间进行插值）和生成（创建输入视图中不可见的新内容）**之间的界限，而这两者在计算机视觉领域历来被视为独立的问题。当RTFM拥有大量输入视图时，由于任务约束较多，它倾向于进行重建；而当输入视图较少时，它则被迫进行超出这些视图范围的推断。

- **持久性**：您可以永远与 RTFM 互动，而这个世界将永不消逝。它构建了一个持久的 3D 世界，即使您转身离开，它也不会消失。
    

RTFM 通过将每一帧建模为在三维空间中具有姿态（位置和方向）来规避这个问题。我们通过使用待生成帧的姿态查询模型来生成新帧。因此，模型对世界（包含在其帧中）的记忆具有空间结构；它将已摆好姿态的帧用作空间记忆。这赋予模型一个弱先验——即它所建模的世界是一个三维欧几里得空间——而无需强制它显式地预测该世界中物体的三维几何形状。

RTFM 的空间记忆功能实现了无限持久化。在生成新帧时，我们会从已生成帧的空间记忆中检索附近的帧，从而为模型构建自定义上下文。我们将此技术称为上下文切换：模型在空间的不同区域生成帧时会使用不同的上下文帧。这使得 RTFM 能够在长时间交互过程中保持庞大的世界，而无需对不断增长的帧集进行推理。

### **Marble**

![[World Model/9572228dd480.png]]

## 强化学习 Dreamer

## 视频生成

  

# **论文笔记**

## WM (NIPS 2018) https://worldmodels.github.io/

**Agent Model**

![[World Model/123abad2ec36.png]]

Train our agent’s controller only inside of the environment generated by its own internal world model, and transfer this policy back into the actual environment.

In this work we look at training a large neural network to tackle RL tasks, by dividing the agent into a large world model and a small controller model.

**The Entire Model**

![[World Model/5721449bef4e.png]]

  

# **知识补充**

### **Covariance-Matrix Adaptation Evolution Strategy (CMA-ES)**

1. **核心：如何“进化”？**
    

CMA-ES 的核心是一个**多元正态分布**。它通过不断调整这个分布的“中心”和“形状”来逼近最优解。

迭代三部曲：

1. **取样 (Sampling)：** 根据当前的正态分布（均值 $\mu$ 和协方差矩阵 $C$），在空间中随机生成一群“候选解”（种群）。
    
2. **选择与评估 (Selection & Evaluation)：** 计算这些候选解的适应度（Fitness），即目标函数值。选出表现最好的那一批“精英”。
    
3. **适配 (Adaptation)：** 根据这些精英的分布，更新下一代的均值（向更好的地方移动）和协方差矩阵（改变搜索的形状和步长）。
    

---

2. **为什么叫“协方差矩阵适配”？**
    

这是该算法最天才的地方。传统的随机搜索通常是圆形（等向性）的，而 CMA-ES 会根据地形调整搜索的**椭圆形状**。

- **捕捉地形：** 如果目标函数的山谷是狭长的，协方差矩阵就会自动伸长，沿着山谷的方向加速搜索。
    
- **二阶近似：** 从数学上看，CMA-ES 实际上是在通过随机取样来隐式地学习目标函数的**海森矩阵（Hessian Matrix）的逆**。这使得它在处理变量之间有强相关性的问题时（即斜坡很歪时），效率远高于普通的随机搜索。
    

2. ### **显式与隐式3D (Explicit Implicit)**
    

![[World Model/444bb3d5c0fc.png]]

![[World Model/c3ac9647e5f4.png]]