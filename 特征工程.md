![[特征工程/6957bf97cd7e.png]]

机器学习数据预处理流程

在工业界，我们常说“数据和特征决定了机器学习的上限，而模型和算法只是逼近这个上限”。以下是针对**用户下单预测场景**的深度梳理：

4. ### 数据采集与特征工程 (Data Collection & Feature Discovery)
    

这是将业务逻辑转化为数学表达的第一步。除了你提到的点，通常分为四个维度：

- **用户侧特征 (User Profile)：** 年龄、性别、城市等级、历史消费等级。
    
- **商品侧特征 (Item Profile)：** 价格、类目、店铺信誉度、近期销量、评价率（好评/差评比）。
    
- **上下文特征 (Context)：** 当前时间（工作日/节假日）、访问设备（iOS/Android）、来源渠道。
    
- **行为特征 (Interaction)：** 近期的点击、收藏、加购、停留时长。
    
- **交叉特征 (Cross Features)：** **核心提效手段**。例如：用户偏好类目与商品类目的匹配度、用户历史平均单价与当前商品价格的比例。
    

---

2. ### 数据格式化与工程化 (Data Formatting & ETL)
    

这是将原始日志转化为模型可读张量的过程，涉及大数据工程：

- **存储与计算：** 关联 Hive 表，利用 Spark/Flink 进行大规模数据聚合。
    
- **时间特征转换：** 将“年月日”转换为数值特征（如：距离大促还有几天、是否在深夜）。
    
- **特征编码 (Encoding)：**
    
    - **类别型：** One-hot 编码或 Embedding（尤其是高维 ID 类特征）。
        
    - **数值型：** 归一化 (Normalization) 或 标准化 (Standardization)，防止模型受量纲影响。
        
- **聚合粒度：** 决定是按“单次点击”作为一行样本，还是按“用户当天行为汇总”作为一行。
    

---

3. ### 数据清洗 (Data Cleaning)
    

在“Garbage In, Garbage Out”的基础上，清洗通常是为了解决以下三类问题：

- **异常值处理：** 比如停留时长超过 24 小时的“挂机”行为，或者秒杀场景下的爬虫数据。
    
- **缺失值填充：** 针对缺失的评价或信誉度，是填 0、填均值，还是作为独立类别处理？
    
- **去重与纠错：** 消除因系统日志重复上报产生的冗余样本。
    
- **业务理解：** 这一步最耗时。例如，你需要识别出哪些点击是“误触”，哪些是“深度浏览”，这直接决定了原材料的纯度。
    

---

4. ### 样本采样与不平衡处理 (Sampling Strategy)
    

电商场景中，下单（正样本）与浏览（负样本）的比例通常在 1:100 甚至更悬殊。

|   |   |   |
|---|---|---|
|策略分类|具体手段|适用场景|
|下采样 (Under-sampling)|丢弃大量负样本，使比例接近 1:3 或 1:10。|负样本极多，且计算资源有限时。|
|过采样 (Over-sampling)|复制正样本，或使用 SMOTE 算法合成虚拟正样本。|正样本极少，容易过拟合时。|
|权重调整 (Cost-sensitive)|在损失函数中加大正样本的权重（Weight）。|L = -\sum (w \cdot y \log(\hat{y}) + (1-y) \log(1-\hat{y}))|
|分层采样 (Stratified)|保证训练集和测试集中的正负比例与原始分布一致。|保持评估指标的真实性。|

5. ### 特征处理
    

#### 5.1 数值型

幅度调整、Log等数据域变化、统计值max, min, mean, std、离散化、Hash分桶、每个类别下对应的变量统计值histogram(分布状况)，以及试试 数值型 => 类别型。

- **归一化** **1）归一化后加快了梯度下降求最优解的速度。** 当使用梯度下降法寻求最优解时，很有可能走“之字型”路线（垂直等高线走），从而导致需要迭代很多次才能收敛；而右图对两个原始特征进行了归一化，其对应的等高线显得很圆，在梯度下降进行求解时能较快的收敛。 **2）归一化有可能提高精度。** 一些分类器需要计算样本之间的距离（如欧氏距离），例如KNN。如果一个特征值域范围非常大，那么距离计算就主要取决于这个特征，从而与实际情况相悖（比如这时实际情况是值域范围小的特征更重要）。 **3）归一化的类型**
    

1. 线性归一化 这种归一化方法比较适用在数值比较集中的情况。这种方法有个缺陷，如果max和min不稳定，很容易使得归一化结果不稳定，使得后续使用效果也不稳定。实际使用中可以用经验常量值来替代max和min。
    

![[特征工程/782030594f1b.png]]

2. 标准差标准化 经过处理的数据符合标准正态分布，即均值为0，标准差为1，其转化函数为：
    

![[特征工程/0806bba1c8db.png]]

其中μ为所有样本数据的均值，σ为所有样本数据的标准差。

3. 非线性归一化 经常用在数据分化比较大的场景，有些数值很大，有些很小。通过一些数学函数，将原始值进行映射。该方法包括 log、指数，正切等。需要根据数据分布的情况，决定非线性函数的曲线，比如log(V, 2)还是log(V, 10)等。
    

在实际应用中，通过梯度下降法求解的模型一般都是需要归一化的，比如线性回归、logistic回归、KNN、SVM、神经网络等模型。

但树形模型不需要归一化，因为它们不关心变量的值，而是关心变量的分布和变量之间的条件概率，如决策树、随机森林(Random Forest)。

#### 5.2 类别型

**One-hot编码/哑变量** One-hot编码一般使用稀疏向量来节省空间，比如只有某一维取值为1，其他位置取值均为0。且需要配合特征选择来降低维度，毕竟高维度会带来一些问题

- 比如K近邻算法中，高维空间下两点之间的距离不好测量；
    
- 再比如logistic回归中，参数的数量会随着维度的增高而增加，容易引起过拟合问题；
    
- 又比如某些分类场景下，只有部分维度有用。
    

**Hash** 而针对一些文本形式的类别型数据时，也可以使用Hash技巧做一些词频统计

**Histogram映射** 还有一种类别型的数据，比如男性女性在很多行为上会有差别，对此，可以使用Histogram映射方法统计男女生的爱好。

#### 5.3 时间型

对于时间型数据既可以看做连续值，也可以看做离散值。比如

- 这些时间型数据可以看做是连续值：持续时间（用户单页的浏览时长）、间隔时间(用户上次购买/点击离现在的时间)。
    
- 这些时间型数据可以看做是离散值：一天中哪个时间段(hour_0-23)、一周中星期几(week_monday...)、一年中哪个星期、一年中哪个季度、工作日/周末等方面的数据。
    

#### 5.4 文本型

- 词袋 如果是文本类型的数据，比如词袋，则可以在文本数据预处理后，去掉停用词，剩下的词组成的list， 在词库中的映射稀疏向量。 第一步：构建词典 第二步：向量化 这个时候会遇到一个问题，比如李雷喜欢韩梅梅，跟韩梅梅喜欢李雷这两句话的含义是不一样的，但用上面那种词袋模型无法区分男追女还是女追男这种顺序。
    
- TF-IDF (词频-逆文档频率) TF: Term Frequency TF(t) = (词t在当前文中出现次数) / (词t在全部文档中出现次数) IDF：IDF(t) = ln(总文档数/ 含t的文档数) 实际使用中，我们便经常用TF-IDF来计算权重，即 TF-IDF = TF(t) * IDF(t) 在词频的基础上，给词加权重。如果一个词在所有文档中都出现（如“的”、“是”），它的重要性就降低；如果只在特定文档出现，它的权重就升高。 字词的重要性随着它在文件中 出现的次数成正比增加，但同时会随着它在语料库中出现的频率成 反比下降
    
- N-gram 模型： 为了保留一点词序信息，不再只统计单个词，而是统计连续的词组（如“watch movies”作为一个整体特征）。
    
- 词嵌入 (Word Embedding / Word2Vec)： 这是目前的深度学习主流。它将词映射到低维、紧凑的稠密向量中，能够捕捉语义（如：Vector(King) - Vector(Man) + Vector(Woman) \approx Vector(Queen)）。
    

#### 5.5 统计型

#### 5.6 组合特征

## Embedding

**Embedding 是将高维、稀疏的离散数据（如单词、ID、图像块），映射到低维、连续的稠密向量空间的过程。**

1. **为什么要用 Embedding？（对比 One-Hot）** 在处理离散特征（如“苹果”、“香蕉”）时，传统做法是 One-Hot 编码，但它有两个致命缺点： **维度灾难**：如果词表有 100 万个词，每个词向量维度就是 100 万，存储和计算压力巨大。 **语义孤岛**：在 One-Hot 空间里，任意两个不同向量的内积都为 0。这意味着模型无法知道“猫”和“狗”比“猫”和“石头”更接近。 Embedding 的出现解决了这些问题： **降维（Dimension Reduction）**：将 100 万维压缩到 256 或 512 维。 **语义捕捉**：将含义相近的对象放在向量空间中靠近的位置。
    
2. **核心特性：** 几何关系代表语义关系在 Embedding 空间中，物体之间的距离和方向具有实际意义。相似度计算：通常使用余弦相似度（Cosine Similarity）来衡量两个向量的接近程度：\text{similarity} = \frac{\mathbf{A} \cdot \mathbf{B}}{\|\mathbf{A}\| \|\mathbf{B}\|}线性平移（Vector Arithmetic）：最经典的例子是 Embedding(\text{King}) - Embedding(\text{Man}) + Embedding(\text{Woman}) \approx Embedding(\text{Queen})。这说明模型学到了“性别”这个抽象概念的向量偏置。