终端工具
1. tmux
tmux (Terminal Multiplexer) 是一款强大的终端复用器。简单来说，它能让你在一个终端窗口中开启多个会话，即便你断开了 SSH 连接，后台运行的任务也不会中断。
掌握 tmux 的核心在于理解它的层级结构：会话 (Session) > 窗口 (Window) > 面板 (Pane)。

---
1. 核心概念：前缀键 (Prefix)
在 tmux 中，所有的快捷键都需要先按一个“前缀键”，默认是 Ctrl + b。
- 操作逻辑：按下 Ctrl + b -> 松开 -> 按下功能键（如 %）。

---
2. 会话管理（基础命令）
这是 tmux 最核心的价值：即使关闭终端窗口，会话依然在服务器后台运行。
暂时无法在飞书文档外展示此内容

---
3. 面板管理（分屏神器）
在一个窗口里同时看日志、写代码、跑程序，全靠面板分割。
- 左右平分面板：Ctrl + b + %
- 上下平分面板：Ctrl + b + "
- 切换面板：Ctrl + b + 方向键
- 全屏/取消全屏当前面板：Ctrl + b + z (Zoom)
- 关闭当前面板：Ctrl + d 或输入 exit

---
4. 窗口管理（类似浏览器标签）
当一个窗口里的面板太多太乱时，可以新建一个窗口。
- 新建窗口：Ctrl + b + c (Create)
- 切换到上/下一个窗口：Ctrl + b + n (Next) 或 p (Previous)
- 通过编号切换：Ctrl + b + 0~9
- 重命名当前窗口：Ctrl + b + ,

---
5. 进阶技巧：翻页模式
在 tmux 中直接滚轮翻页通常会失效。你需要进入复制模式：
6. 按下 Ctrl + b + [。
7. 使用方向键或 PageUp/PageDown 滚动。
8. 按 q 退出该模式。

9. scp和rsync
暂时无法在飞书文档外展示此内容

3. 下载软件
- Appimage
第一步：赋予执行权限
打开终端，进入文件所在目录，执行：
chmod +x <文件名>.AppImage
第二步：运行它
你可以直接在终端启动：
./<文件名>.AppImage
或者在文件管理器（Nautilus）里右键 -> 属性 -> 权限 -> 勾选“允许作为程序执行文件”，之后就可以双击运行了。
常见报错：缺少 FUSE
如果你用的是 Ubuntu 22.04 或更高版本，运行 AppImage 可能会报错：
dlopen(): error loading libfuse.so.2
这是因为新版 Ubuntu 默认不再安装过时的 libfuse2。解决办法：
sudo apt update
sudo apt install libfuse2

4. SSH错误
这个问题是因为你的本地主机（Ubuntu 终端）记录的服务器 SSH 公钥指纹与服务器当前返回的指纹不匹配。
这通常发生在服务器重装、迁移、域名重新绑定或 SSH 服务重新配置后。为了安全起见，SSH 客户端会拦截连接以防止潜在的中间人攻击（MITM）。
解决方案
你只需要按照提示，从本地的 known_hosts 文件中删除旧的记录，然后重新连接即可。

---
第一步：清理旧的密钥记录
在终端中执行报错信息中提供的命令：
Bash
ssh-keygen -f '/home/agiuser/.ssh/known_hosts' -R '[code.iqiyuan-robot.com]:2222'
这条命令会找到 known_hosts 文件中第一行（:1）关于该域名的旧指纹并将其移除。
第二步：重新克隆并更新指纹
再次尝试执行 git clone 命令：
Bash
git clone ssh://git@code.iqiyuan-robot.com:2222/qiyuan-research/xr-1.git
第三步：确认指纹