# 学习规划

> 边学边看，不做完美计划。每条学完一个阶段再定下一步。

---

## 当前定位

- EE 本科 → 机器人硕士，2026 秋季入学
- 技术方向：具身智能 / robot learning
- 两面基础都薄弱：VLA 缺 CS 系统工程能力，RL 缺数学和算法直觉
- **策略**：研一两边同时补基础，看自己对"调分布式训练"还是"推 RL 公式"更有感觉，再定重心

---

## 两条路线

| | VLA / World Model | RL |
|------|------|------|
| **就业** | 岗位多、薪资高、离落地近 | 岗位少（集中在自动驾驶/量化/lab） |
| **研究** | 空间在收窄，核心架构不是普通人能改的 | 大量 open problems，一个博士+工作站就能发顶会 |
| **需要的功底** | 分布式训练、GPU 显存、数据工程 | 概率论、优化理论、控制理论 |
| **我的短板** | infra 经验为零 | 数学底子薄但在补 |

> 长期看：RL 背景 + 会写 PyTorch infra = 比纯 VLA 选手多一种解法。不必二选一，但要有一个主力方向。

---

## PyTorch 学习路径

目标不是"会用 `nn.Linear`"，而是能 debug 训练过程中不报 trace、但也不 work 的问题。

### 阶段 1：读懂一个训练脚本

找一篇有官方代码的 paper（任意），把 `train.py` 从头到尾啃透：

- 数据从哪进、怎么 transform、怎么 batch
- loss 在哪算的、`backward()` 在哪调用的
- optimizer.step() 前后发生了什么
- checkpoint 怎么存的、怎么 resume

### 阶段 2：故意破坏它

每个实验半天，看怎么挂的：

- [ ] batch size 调到 1 → BatchNorm 为什么崩
- [ ] `model.train()` 换成 `model.eval()` → BN/dropout 不动的影响
- [ ] 删掉 `optimizer.zero_grad()` → 梯度累积的诡异 loss 曲线
- [ ] 关掉 mixed precision → 显存多占多少
- [ ] 学习率 ×10 → loss 怎么炸的
- [ ] `torch.manual_seed(0)` 删掉 → 两次跑出来的 loss 曲线不一样

### 阶段 3：加 infra

- [ ] 单卡 → DDP（`torchrun --nproc_per_node=4`）
- [ ] 加 gradient checkpointing（`torch.utils.checkpoint`）
- [ ] 用 `torch.profiler` 找到 bottleneck（data loading 堵了还是 kernel launch 慢了）
- [ ] 改 `num_workers`、加 `prefetch_factor`，看训练速度变化

### 阶段 4：写一个 backward

挑一个简单激活函数，手写 `autograd.Function`：

```python
class MyReLU(torch.autograd.Function):
    @staticmethod
    def forward(ctx, x):
        ctx.save_for_backward(x)
        return x.clamp(min=0)

    @staticmethod
    def backward(ctx, grad_output):
        x, = ctx.saved_tensors
        grad_input = grad_output.clone()
        grad_input[x < 0] = 0
        return grad_input
```

用 `torch.autograd.gradcheck` 验证梯度。写完你对 autograd graph + `ctx.save_for_backward` 就不怕了。

---

## RL 基础路线

### 先啃书

1. Sutton & Barto 前四章（MDP → DP → MC → TD）——这四章通了，RL 语言就通了
2. 第五章起（Q-Learning、SARSA、eligibility traces）——算法工具箱
3. 第十三章（Policy Gradient）——PPO 的前置

### 再动手

- [ ] OpenAI Gym 跑一个 DQN / REINFORCE，手写不调包
- [ ] 把 reward 改一下 → 看策略怎么变 → 理解 reward shaping 为什么难
- [ ] 复现 PPO（别用 stable-baselines3，自己写）

---

## 论文阅读习惯

- **从哪里开始**：近两年 ICRA + CoRL proceedings，看标题 → 感兴趣就 abstract → 想深入就读全文
- **读不是目的**：每读完一篇有一句话总结，写在论文索引里
- **顺藤摸瓜**：一篇 paper 引用链往上追 2-3 篇，往下追 2-3 篇 related work，形成一个小树

---

## 补 CS 系统基础

| 需要会的 | 优先级 | 怎么学 |
|---------|:------:|------|
| Linux 命令行 | 高 | CS_Note 工具笔记 + 日常用 |
| Git | 高 | 日常用 |
| Docker | 中 | CS_Note 工具笔记 + 跑一个 ROS2 容器 |
| CMake | 中 | CS_Note 工具笔记 |
| CUDA/GPU 编程基础 | 低（先会用再说） | 等项目需要时再学 |

---

## 待定

- [ ] 选哪个 lab / 导师
- [ ] 研一选课策略
- [ ] 确定主力方向（VLA or RL）
