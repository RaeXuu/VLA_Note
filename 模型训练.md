## **训练工具**

### **W&B (Weights & Biases)**

|   |   |
|---|---|
|功能|解决的痛点|
|系统监控|自动记录 GPU 温度、显存占用、CPU 使用率，防止硬件过热或显存溢出。|
|产物管理 (Artifacts)|自动保存每个版本的模型权重（.pth 或 .onnx）和数据集，实现实验可追溯。|
|交互式表格|在网页端直接筛选、对比不同实验跑出的效果图，不用再一个个翻本地文件夹。|
|团队协作|实验结果链接可以直接发给同事，网页端实时刷新，像刷朋友圈一样看训练进度。|

- **训练的参数**
    

|   |   |   |
|---|---|---|
|参数名称 (Parameter)|核心定义 (Definition)|关键技术含义与预警 (Significance)|
|param_norm|模型所有权重的 $L_2$ 范数，代表参数空间的“长度”或数值总量。|含义：反映模型权重的更新规模。若曲线呈指数级飙升，通常预示着训练即将崩溃或严重过拟合。目前表现非常平稳。|
|loss|训练损失函数值，衡量模型当前输出与专家演示（GT）的差距。|含义：最直观的收敛指标。当前曲线说明模型已经渡过了快速学习阶段，进入了深度的微调或已达瓶颈。|
|grad_norm|梯度范数，衡量每次迭代时参数更新的幅度。|含义：反映训练稳定性。低位波动是理想状态，说明模型找到了平缓的局部极小值区域。若此处突然跳变，通常意味着遇到了“坏数据”或学习率过大。|
|action_mse|动作均方误差（MSE），衡量模型预测动作与专家动作的欧氏距离。|含义：针对机器人任务的核心指标。在模仿学习中，loss 下降不一定代表任务成功率提升，但 action_mse 必须足够低。|

- **训练过程中GPU的硬件参数**
    

|   |   |   |   |
|---|---|---|---|
|指标名称 (Parameter)|核心定义 (Definition)|图表曲线表现 (Observation)|状态解读与预警 (Insight)|
|Memory Errors (Uncorrected / Corrected)|内存错误统计。Corrected 是通过 ECC 纠正的软错误；Uncorrected 是无法纠正的严重错误。|曲线全为 0（水平直线）。|完美状态。说明显存硬件非常健康，没有发生由于高温或老化导致的位翻转，训练数据是可靠的。|
|Memory Clock Speed (MHz)|显存频率。显存读写数据的速度。|极度稳定，保持在约 3200 MHz 的恒定直线。|正常。显存频率通常不会像核心频率那样剧烈波动，稳定的直线说明显存控制器工作正常。|
|SM Clock Speed (MHz)|流处理器（核心）频率。GPU 进行矩阵运算时的实时时钟频率。|剧烈波动。在 1200 到 1900 MHz 之间反复跳动，呈现“毛刺”状。|关键信号。这种波动通常意味着 GPU 在等待数据（I/O 瓶颈）。当 CPU 加载数据或预处理变慢时，GPU 核心会降频空转，导致波动。|
|Power Usage (W)|GPU 实时功耗（瓦特）。反映了 GPU 运算的强度。|波动上升。峰值达到 700W 左右（通常是单机多卡的总功耗或高性能单卡）。|状态同步。功耗的波动趋势与 SM 频率完全一致，说明在运算波峰时功耗瞬间拉高。|
|Power Usage (%)|功耗百分比。当前功耗占额定最大功耗的比例。|频繁触及 100%，但随后又迅速跌落。|负载不稳。虽然 GPU 能跑到满载，但无法持续。理想的训练状态应该是长期维持在 80%-95% 的平滑区域。|

  

## **训练检测硬件状态的终端指令**

1. ### GPU 使用情况查询
    

这是深度学习开发者最常用的工具，用于查看显存占用、核心频率、功耗和温度。

|   |   |   |
|---|---|---|
|指令|说明|推荐指数|
|nvidia-smi|最标准。显示显卡型号、驱动版本、显存占用、温度、功耗和正在运行的 PID。|⭐⭐⭐⭐⭐|
|watch -n 1 nvidia-smi|实时刷新。每隔 1 秒执行一次 nvidia-smi，适合观察训练动态变化。|⭐⭐⭐⭐⭐|
|nvtop|最直观。类似于任务管理器的彩色交互界面，可以看到 GPU 频率和显存的实时趋势图。|⭐⭐⭐⭐|

> **提示**：如果系统中没有 `nvtop` 或 `gpustat`，可以通过以下命令安装： `sudo apt install nvtop` 或 `pip install gpustat`

---

2. ### 系统内存 (RAM) 与 CPU 查询
    

在数据预处理阶段，监控系统内存和 CPU 负载能帮你判断 DataLoader 是否是瓶颈。

|   |   |   |
|---|---|---|
|指令|说明|推荐指数|
|free -h|最快查询。-h 表示以人类可读的格式（G/M）显示当前已用、可用内存。|⭐⭐⭐⭐|
|htop|交互式神器。彩色显示每个 CPU 核心的占用、内存使用条，支持搜索进程、结束进程。|⭐⭐⭐⭐⭐|

  

## **训练的分布式框架**

1. ### **DDP (Distributed Data Parallel) —— 数据并行**
    

这是 PyTorch 最常用、最经典的分布式模式。

- **核心逻辑：** **模型分身，数据切片。**
    
    - 每块 GPU 上都加载一个完整的模型副本。
        
    - 训练数据被分成 N 份（N = GPU 数量），每块显卡吃一份。
        
    - **同步：** 每块显卡算出自己的梯度后，通过 `All-Reduce` 算法进行全局同步，确保更新后的模型权重在所有显卡上依然是一模一样的。
        
- **适用场景：** 模型不算太大，**单块显卡的显存能放下整个模型**（包括权重、优化器状态和激活值）。
    
- **优点：** 速度快，通信开销相对较小，配置简单。
    

---

2. ### **MP (Model Parallelism) —— 模型并行**
    

当模型特别大（比如几十亿、上百亿参数），单块显卡的显存连模型权重都放不下时，DDP 就失效了。这时需要 MP。

模型并行通常分为两种子模式：

#### A. TP (Tensor Parallelism, 张量并行)

- **做法：** 把一个大矩阵乘法（比如 Transformer 里的 Linear 层）拆开，让两块显卡各算一半。
    
- **特点：** 通信极其频繁，通常要求 GPU 之间有高性能连接（如 NVLink）。
    

#### B. PP (Pipeline Parallelism, 流水线并行)

- **做法：** 把模型的层（Layers）拆开。比如第 1-10 层放在 GPU 0，第 11-20 层放在 GPU 1。
    
- **特点：** 像工厂流水线一样工作，但容易产生“气泡”（即后面的显卡在等前面的显卡算完），需要特定的调度算法（如 GPipe）。
    

---

3. ### **FSDP (Fully Sharded Data Parallel) —— 全分片数据并行**
    

这是目前大模型训练的“黄金标准”，由 PyTorch 官方推出，吸收了微软 DeepSpeed ZeRO 技术的精髓。

- **痛点：** DDP 虽然快，但每块显卡都存一份完整的模型，太浪费显存；MP 虽然能存大模型，但开发和通信太复杂。
    
- **FSDP 的做法：** **平时化整为零，计算时聚沙成塔。**
    
    - 它把模型参数、梯度、优化器状态全部“打碎（Shard）”分布在所有 GPU 上。
        
    - 只有在某一层需要计算时，才通过快速通信临时把这一层的参数拉过来，算完立刻释放。
        
- **优势：** 既能像 DDP 一样高效处理大规模数据，又能像 MP 一样承载超大模型。
    

  

## **训练过程中常见的问题**

1. ### 训练提前结束
    

在深度学习训练中，程序提前停止（Early Termination）通常可以归纳为**主动触发（逻辑设计）**、**环境故障（外部崩溃）**以及**模型数值异常（逻辑错误）**三大类。

以下是常见的几种原因：

---

- 主动触发的逻辑（预期内停止）
    

这是为了节省算力和防止过拟合而设计的机制。

**早停法 (Early Stopping)：**

  **原理：** 监控验证集上的指标（如 Validation Loss 或 Accuracy）。如果连续 $N$ 个 Epoch 指标没有提升（Patience），程序会自动停止。

  **现象：** 训练日志中通常会打印类似 `Early stopping triggered` 的信息。

**达到预设限制：**

  代码中设置了 `max_steps` 或 `max_epochs`。

  设置了训练时长限制（例如在 SLURM 集群中设置了 `--time`）。

---

- 数值异常（导致崩溃）
    

这是最常见的一类技术故障，通常与**表征学习**中的梯度稳定性有关。

**梯度爆炸/消失 (NaN/Inf)：**

  **原因：** 学习率过高、初始化不当或数据中存在异常值，导致 Loss 变成 `NaN` 或 `Inf`。

  **表现：** 很多框架（如 PyTorch Lightning 或 Accelerator）在检测到 `NaN` 时会强制停止训练以防污染模型。

**Loss 不收敛或异常波动：**

  如果代码逻辑中有类似 `assert loss < threshold` 的检查，或者 Loss 长期不下降触发了某些自定义的保护逻辑。

---

- 环境与硬件故障（非预期停止）
    

**显存溢出 (OOM - Out of Memory)：**

  **原因：** 随着训练进行，缓存的数据（如存储的梯度、Activation）超出了显存容量。

  **表现：** 终端报错 `RuntimeError: CUDA out of memory`。

**内存溢出 (Dataloader OOM)：**

  **原因：** CPU 内存被耗尽，通常是因为 `num_workers` 设置过高或 Data Pipeline 中存在内存泄漏。

  **表现：** 进程被系统 `Out of memory killer` 直接杀死。

**磁盘空间耗尽：**

  **原因：** 频繁保存 Checkpoints 或 Checkpoint 文件太大，导致磁盘满了。

  **表现：** 报错 `OSError: [Errno 28] No space left on device`。这也是 W&B 同步出错的常见诱因。

---

- W&B 与 进程管理问题
    

**W&B 服务中断：**

  如果 W&B 的后台进程崩溃（例如你遇到的 `AssertionError`），有时会导致主训练进程在尝试通信时挂起或退出。

**集群预取/抢占 (Preemption)：**

  如果你在公有云或共享集群上运行，任务可能因为优先级不够被系统挂起（Spot Instance）。

**进程间通信 (IPC) 失败：**

  多卡训练 (DDP) 时，如果某一张卡卡死或心跳超时，主进程会关闭整个 Job。

---

排查建议表

|   |   |
|---|---|
|检查项|动作|
|Log 日志末尾|搜索 Error, Exception, NaN, Killed 等关键字。|
|W&B 指标|观察 Loss 曲线在停止前是否突然飙升或变成直线。|
|硬件资源|使用 df -h 查磁盘，top 或 htop 查内存。|
|验证集表现|确认是否是指标不再提升触发了 Early Stopping。|

---

  

  

2. ### 训练曲线不重合
    

它们极大概率会在同一条“大趋势”曲线上，但由于学习动力学（Learning Dynamics）的复杂性，可能会出现阶段性的“偏移”或“转折”。

为了理解 `param_norm`（参数范数，通常指 L2 范数 ∥θ∥2）随数据量增加的变化，我们需要从以下几个维度来分析：

---

- 模型的“收敛点”漂移
    

当你增加数据量（500 → 1000 → ...）时，模型的**损失函数曲面（Loss Landscape）**是在不断变化的。

**小数据量 (500)：** 约束较少，模型很容易进入一个极其平坦或极其“偷懒”的局部最优解。此时参数范数可能较小（因为没学到什么东西），也可能由于过拟合而变得极大（权重爆炸以拟合噪声）。

**大数据量 (2000)：** 约束增加，模型必须学习更复杂的特征来降低训练误差。

**结论：** 随着数据量增加，模型需要“用力”去拟合更多样化的模式，通常会导致 `param_norm` 呈现**单调递增后趋于稳定**的态势。如果数据分布一致，这些点通常会落在一条平滑的饱和曲线（Saturation Curve）上。

---

- 训练步数与正则化的影响
    

这是导致曲线“不连续”的主要技术原因：

**训练时长：** 如果你对 500 个样本训练 100 epoch，和对 2000 个样本训练 100 epoch，总的参数更新步数（Steps）是不一样的。

  如果固定 Epoch，2000 样本的更新步数更多，`param_norm` 往往会更高。

  如果固定总 Steps，它们更有可能落在同一条曲线上。

**Weight Decay (L2 正则化)：**

![[模型训练/03315c947b31.png]]

正则化项会不断将 `param_norm` 拉向零。当梯度带来的“拉力”与正则化的“推力”达到平衡时，范数会稳定。数据量越大，梯度信号通常越稳定，这个平衡点会更靠右。

---

- 为什么可能会“不在一个曲线上”？
    

如果在实验中发现 1500 和 2000 的范数突然跳出了之前的趋势，通常是因为发生了**相变（Phase Transition）**：

**特征过载：** 当数据量达到某个阈值（例如 1500），模型可能从“记忆样本”转向“学习通用规律”。这种策略转换会导致权重分布发生剧烈变化。

**学习率敏感度：** 同样的学习率在小数据和大数据下的表现不同。如果大数据量导致梯度范数（Gradient Norm）变大，而你没有调整学习率，参数范数可能会非线性增长。

**初始化权重：** 除非你每次都用**完全相同的 Seed 和初始权重**，否则随机性可能导致它们落在 Loss Landscape 的不同盆地里。

  

3. ### 网速带宽
    

- **单位陷阱：Mbps vs MB/s**
    
- **硬件“木桶效应”排查清单**
    
- 网线类别：看看网线上印的字。Cat5（五类线） 最高只支持 100Mbps。
    
- 路由器/交换机端口：很多旧路由器的 WAN/LAN 口是“百兆口”
    
- 网卡协商速率：在 Linux 终端输入 `ethtool eth0`（或你的网卡名），检查 `Speed:` 是否显示为 `1000Mb/s`。如果是 `100Mb/s`，说明某处硬件限制了。
    
- 磁盘写入速度：虽然现在的 SSD 很快，但如果是往挂载的旧机械硬盘或是慢速 USB 闪存盘里下载，硬盘写入跟不上，下载速度也会被拖累。
    
- **`rsync`** **场景的特殊限制**
    
- SSH 加密开销： `rsync` 默认走 SSH 协议。SSH 会对传输的数据进行加密和解密，这非常消耗 CPU 单核性能。
    
- 服务端限速： 你连接的是 `10.114.4.33`。如果那台服务器的出口带宽被限制了，或者同时有很多人在跑任务/下数据，你分到的速度自然就慢。
    
- 小文件过多： 如果模型文件夹里包含大量碎文件，`rsync` 在扫描和比对文件上花的时间远比实际传输多。